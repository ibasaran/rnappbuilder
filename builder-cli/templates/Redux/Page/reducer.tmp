
export default function (state={}, action) {
    switch (action.type) {
        case <%data.dataName%>:
            return {...state, <%data.dataName%>:action.<%data.dataName%>};
        default:
            return state;
    }
}